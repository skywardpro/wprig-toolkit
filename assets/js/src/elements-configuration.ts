/**
 * File elements-configuration.ts.
 *
 * Handles elemnts TypeScript for your theme.
 */

import { initExampleElement } from './elements/exampleElement';

document.addEventListener('DOMContentLoaded', () => {
	initExampleElement();
});
